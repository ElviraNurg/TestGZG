/* export default {
    build: {
      outDir: 'dist', // имя общей выходной директории
      assetsDir: 'assets' // имя директории со статикой
      
    }
  } */
    import { defineConfig } from 'vite';
    
    
    export default defineConfig({
     base:'./'
    });